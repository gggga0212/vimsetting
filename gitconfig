[user]
    name = ABC
    email = ABC@ABC.com
[core]
    editor = vim
	autocrlf = true
    pager = diff-so-fancy | less --tabs=4 -RFX
[alias]
    co = checkout
    st = status
    br = branch -a
    ls = log --stat --graph
    sub = submodule
    cotest = checkout host_stack_test
    coteam = checkout host_stack_team
    autocrlf = config --global core.autocrlf
[credential]
	helper = store
[color "diff"]
	meta = 11
	frag = magenta bold
	old = red bold
	new = green bold
	func = 146 bold
	commit = yellow bold
	whitespace = red reverse
[interactive]
    diffFilter = diff-so-fancy --patch
[color]
	ui = true
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
